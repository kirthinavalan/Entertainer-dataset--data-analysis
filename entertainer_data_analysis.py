# -*- coding: utf-8 -*-
"""Entertainer_data analysis.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1jGnuu2nUx3Wo4KilMEnVbNRRlibPGa97
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
sns.set()

"""Load the data"""

raw_data=pd.read_excel('/content/Entertainer - data.xlsx')

raw_data.head()

data=raw_data.copy()

data.shape

data.isnull().sum()

"""Observation : considering the null values only in Year of death
data is have no missing value
"""

data.info()

"""Gender histogram"""

plt.figure(figsize=(11,8))
sns.countplot(data=data, x='Gender (traditional)', alpha=0.9, color='Blue')
plt.legend()
plt.xlabel('Gender')
plt.ylabel('Count')
plt.show()

plt.figure(figsize=(10,8))
plt.hist(data['Year of Breakthrough/#1 Hit/Award Nomination'], color='RED')
plt.hist(data['Year of First Oscar/Grammy/Emmy'], color='Yellow', alpha=0.5)

plt.show()

f.shape

"""Observation : considerable after breakthrough , roughly in span of 2 to 5 year, entainers have their oscar and other awards"""

data['AGE AT Year of First Oscar/Grammy/Emmy']=data['Year of First Oscar/Grammy/Emmy']-data['Birth Year']

data.head()

plt.figure(figsize=(10,8))
plt.hist(data['AGE AT Year of Breakthrough/#1 Hit/Award Nomination'], color='RED')
plt.hist(data['AGE AT Year of First Oscar/Grammy/Emmy'], color='Green', alpha=0.5)

plt.show()

"""MOST of the entertainer has their breeakthrough from mid of 20's to mid 30's
which seem like crucial age of to be succesful entertainer but there is always few exception (outliers)
"""

paas= data[data['Year of Death']>0]
paas

plt.scatter(paas['Year of Last Major Work (arguable)'],paas['Year of Death'])

paas.corr()